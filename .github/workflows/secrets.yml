    name: secrets
    on: 
        - push
    jobs:
        secret:
            runs-on: ubuntu-latest
            steps:
                - name: using github secret
                  run: |
                    echo "secret: ${{secrets.API_KEY}}"
                    echo "variable: ${{vars.VARIABLE}}"